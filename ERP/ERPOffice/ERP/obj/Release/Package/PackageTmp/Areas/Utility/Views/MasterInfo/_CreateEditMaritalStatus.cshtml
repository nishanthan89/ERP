@model ERP.Utility.Models.MaritalTypeBO

@using (Ajax.BeginForm("CreateMaritalStatus", "MasterInfo", new AjaxOptions { HttpMethod = "POST", OnSuccess = "SaveMarital" }))
{
    @Html.AntiForgeryToken()
    <br />
    <div class="form-horizontal well">        
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.MaritalId)
        <div>
            <div class="form-group">
                @Html.LabelFor(model => model.MaritalStatus, htmlAttributes: new { @class = "control-label col-md-4" })
                <div class="col-md-8">
                    @Html.EditorFor(model => model.MaritalStatus, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.MaritalStatus, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                <div class="col-md-offset-4 col-md-8">
                    <input type="submit" value="Create" id="CreateId" class="btn btn-success" data-toggle="tooltip" title="Save the Marital Status" />
                    <input type="submit" value="Update" id="UpdateId" class="btn btn-success" data-toggle="tooltip" title="Update the Marital Status" hidden="hidden" />
                    <input type="button" value="Cancel" class="btn btn-default" data-toggle="tooltip" title="Cancel the Marital Status creation" role="button" onclick="CancelButton()" />
                </div>
            </div>
        </div>

    </div>
}

<script>

    var condition =@Model.MaritalId;
    if (condition == 0)  {

        $('#CreateId').show();
        $('#UpdateId').hide();
    } else {

        $('#CreateId').hide();
        $('#UpdateID').show();
    }
</script>

