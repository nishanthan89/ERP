@model ERP.Utility.Models.EthnicTypeBO

@using (Ajax.BeginForm("CreateEthnicType", "MasterInfo", new AjaxOptions { HttpMethod = "POST", OnSuccess = "SaveEthnicType" }))
{
    @Html.AntiForgeryToken()
    <br />
    <div class="form-horizontal well">      
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.EthGrpID)

        <div>
            <div class="form-group">
                @Html.LabelFor(model => model.EthGrpName, htmlAttributes: new { @class = "control-label col-md-4" })
                <div class="col-md-8">
                    @Html.EditorFor(model => model.EthGrpName, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.EthGrpName, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                <div class="col-md-offset-4 col-md-8">
                    <input type="submit" value="Create" id="CreateId" class="btn btn-success"  data-toggle="tooltip" title="Save the Ethnic Creation" />
                    <input type="submit" value="Update" id="UpdateId" class="btn btn-success" data-toggle="tooltip" title="Update the Ethnic" hidden="hidden" />
                    <input type="button" value="Cancel" class="btn btn-default" data-toggle="tooltip" title="Cancel the Ethnic Creation" role="button" onclick="CancelButton()" />
                </div>
            </div>
        </div>

    </div>
}

<script>

    var condition =@Model.EthGrpID;
    if (condition == 0)  {

        $('#CreateId').show();
        $('#UpdateId').hide();
    } else {

        $('#CreateId').hide();
        $('#UpdateID').show();
    }
</script>
